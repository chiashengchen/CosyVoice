--extra-index-url https://download.pytorch.org/whl/cu121

# Core inference deps for CosyVoice2
torch==2.4.0
torchaudio==2.4.0
torchvision==0.19.0

# CosyVoice model loading
# transformers 4.44.2 is compatible with torch 2.4.x; CosyVoice uses register_fake which needs torch>=2.4
transformers==4.44.2
huggingface_hub
modelscope==1.20.0
omegaconf==2.3.0
hydra-core==1.3.2
HyperPyYAML==1.2.3

# TTS inference
conformer==0.3.2
x-transformers==2.11.24
diffusers==0.29.0
onnxruntime-gpu==1.18.0; sys_platform == 'linux'
# openai-whisper installed separately with --no-deps to avoid triton version conflict
# (torch>=2.4 needs triton==3.0.0; whisper's metadata says triton<3, but whisper never imports triton)
pyarrow
pyworld==0.3.4
soundfile==0.12.1
librosa==0.10.2
wetext==0.0.4

# Server
fastapi==0.115.6
uvicorn==0.30.0
numpy==1.26.4
